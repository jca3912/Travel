# Credenciales: NUNCA se suben. En la nube van como secretos del repo.
.env

# Informes: se regeneran en cada pasada, no hace falta versionarlos.
reports/

__pycache__/
*.pyc
.venv/

# OJO: data/prices.db SÍ se versiona a propósito.
# Es el histórico acumulado, el activo del proyecto. Sin él, el agente de la
# nube arrancaría cada día sin línea base y la detección estadística no
# funcionaría nunca.
